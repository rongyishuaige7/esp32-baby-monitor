# 安全、隐私与不适用场景

## 严格不适用场景

- 本项目不是婴儿监护、睡眠安全、姿态识别、哭声识别、医疗、健康监测、生命体征、紧急告警或无人值守看护设备。
- 不得依据 LED、蜂鸣器、串口、HTTP JSON、距离组合、声音幅度、温湿度或构建结果作出婴儿姿态、健康、安全、风险、告警送达或有人正在看护的结论。
- 不得将本项目用于儿童、婴儿、医疗、生命安全、紧急响应或任何需要可靠报警/人工照看的场景。

## 凭据与个人数据

- `src/wifi_credentials.h` 只能保存在本机，已被 Git 忽略。绝不提交、截图、录屏、粘贴到 Issue 或写入串口日志。
- 不要公开 SSID、密码、私网 IP、MAC、家庭地址、姓名、儿童影像、声音、健康信息或其他个人数据。
- 若提供实物照片、视频或日志，先去除 EXIF/GPS、显示屏中的凭据/网络信息和可识别个人的信息。

## 网络边界

- 成功连接 Wi-Fi 后，固件提供无 TLS、无认证、无访问控制和无审计的本地 HTTP 服务。
- 它只可用于隔离且可信的学习局域网，不能暴露到公网、端口转发、共享热点或不可信网络。
- `/api/mute` 只切换本机蜂鸣器状态；HTTP 200 不代表通知、响应、看护或安全处置已经发生。

## 硬件边界

- HC-SR04 ECHO 常为 5 V，必须使用正确的分压/电平转换后才能连接 ESP32 GPIO。
- 所有模块必须确认额定电压、电平、供电、电流、限流、驱动和公共地；不要用 GPIO 直接驱动大电流负载。
- 未经当前 commit 的实物复测，不要把源码引脚、阈值或 LED 映射写成已验证接线或可靠行为。

## 报告问题

请勿在公开 Issue 中提交真实凭据、儿童相关材料、私网信息或可识别个人的数据。可通过 GitHub 私密安全报告，或维护者公开资料中提供的渠道，提交最小化且已脱敏的复现说明。
