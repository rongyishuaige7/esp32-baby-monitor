# 硬件与电气边界

本说明只整理当前公开源码中的引脚常量与输出假设，不是原理图、PCB、实物照片、已验证接线或安全设计。原工程没有可公开核验的 EDA、PCB、Gerber、物料铭牌或当前真机记录，因此所有“实际模块型号/接法”均保持待确认。

| 模块 / 信号 | 代码引脚 | 源码可确认 | 真机仍需确认 |
| :-- | :-- | :-- | :-- |
| HC-SR04 #1 | TRIG GPIO5，ECHO GPIO18 | 读距离的程序入口 | 模块型号、供电、安装位置、ECHO 分压/电平转换、共地与有效距离 |
| HC-SR04 #2 | TRIG GPIO19，ECHO GPIO21 | 第二路读距离入口 | 同上；两路相互干扰、安装方向和读数稳定性 |
| DHT11 | DATA GPIO4 | Adafruit DHT 库读取 | 模块版本、供电、DATA 上拉、时序和读数合理性 |
| 模拟声音模块 | AO GPIO34 | ADC 峰峰值采样 | 模块型号、输出摆幅、ADC 衰减/标定、环境噪声与任何语义解释 |
| RGB LED | R GPIO25，G GPIO27，B GPIO26 | 代码按低电平点亮 | 共阳/共阴、限流电阻、实际颜色、亮度与电流 |
| 蜂鸣器 | GPIO2 | 代码按低电平触发 | 有源/无源、额定电压、电流、驱动管、续流/保护和实际响声 |
| 查询按钮 | GPIO22 | 高电平上升沿触发状态打印 | 外部上拉/下拉、电平、去抖、输入不悬空 |
| 蜂鸣器切换按钮 | GPIO23 | 高电平上升沿切换静音 | 外部上拉/下拉、电平、去抖、输入不悬空 |

## 电气前置条件

1. **断电接线。** 修改接线、探测引脚或调整电平前，先断开 USB 和外部电源。
2. **ESP32 GPIO 不是 5 V 容忍。** HC-SR04 ECHO 和任何第三方数字/模拟输出若可能达到 5 V，必须先按实际电路完成正确分压或双向/单向电平转换。不要直接连入 ESP32。
3. **确认供电与共地。** 传感器和执行器的电压、电流、峰值负载、公共地和 USB 供电能力都须以实物为准。大电流负载需独立受控驱动和适当电源设计。
4. **输入不能悬空。** 当前按钮代码使用 `INPUT` 并假定按下为高电平；实物必须提供稳定外部上拉/下拉。若实际采用 `INPUT_PULLUP` 的低电平按键，应先修改代码、重新构建并真机复测。
5. **输出极性待核。** 当前 LED 与蜂鸣器按低电平点亮/触发编码；不匹配的电路可能反相、常亮或无响应。

## 接线图的范围

`hardware/wiring-diagram.svg` 是根据源码生成的**接线边界图**，刻意标出了 5 V ECHO 不可直连、外部按钮上下拉和驱动/限流待确认。它不代替原理图、PCB 或制造文件，且不能用于推断任何人的姿态、安全或健康状态。
